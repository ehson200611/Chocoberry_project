# 🚀 Быстрое исправление - Отправка кода на GitHub

## Проблема
GitHub не принимает пароли для HTTPS. Нужен **Personal Access Token**.

## ✅ Решение (3 простых шага):

### Шаг 1: Создайте Personal Access Token

1. Откройте в браузере: **https://github.com/settings/tokens**
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note**: `Chocoberry_project`
   - **Expiration**: выберите срок (например, 90 дней)
   - **Select scopes**: отметьте ✅ **`repo`** (полный доступ)
4. Нажмите **"Generate token"** внизу страницы
5. **⚠️ ВАЖНО:** Скопируйте токен сразу! (он показывается только один раз)
   - Токен выглядит примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 2: Отправьте код с токеном

Выполните команду:
```bash
git push --set-upstream origin main
```

Когда Git попросит:
- **Username**: `ehson200611`
- **Password**: **вставьте ваш Personal Access Token** (не пароль от GitHub!)

### Шаг 3: Готово! 🎉

После успешной отправки токен сохранится, и больше не нужно будет его вводить.

---

## 🔄 Альтернатива: Использовать скрипт

Или просто запустите:
```bash
bash fix_github_auth.sh
```

Скрипт проведет вас через все шаги.

---

## 📝 Если не работает:

1. Убедитесь, что репозиторий создан на GitHub: https://github.com/ehson200611/Chocoberry_project
2. Проверьте, что токен имеет право `repo`
3. Попробуйте использовать SSH вместо HTTPS:
   ```bash
   bash setup_ssh_github.sh
   ```

